export const metadata = {
  title: "AI Body Scan | Protocol",
  description: "See what your body scan reveals about your proportions, composition, and attractiveness potential.",
};

export default function ScanLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
